# ============================================
# CRM HỆ THỐNG - FILE CẤU HÌNH MÔI TRƯỜNG FRONTEND
# ============================================
# 
# HƯỚNG DẪN SỬ DỤNG:
# 1. Sao chép file này thành .env: cp env.example .env
# 2. Sao chép file này thành .env.production: cp env.example .env.production
# 3. Điền các giá trị phù hợp cho môi trường của bạn
# 4. KHÔNG commit file .env vào Git (đã được thêm vào .gitignore)
# 
# LƯU Ý:
# - File .env: Dùng cho môi trường development
# - File .env.production: Dùng cho môi trường production (khi build)
# - File .env.local: Dùng cho cấu hình local (ưu tiên cao nhất, không commit)
# 
# ============================================

# ============================================
# CẤU HÌNH API BACKEND
# ============================================
# URL cơ sở của API backend
# Giá trị: URL đầy đủ của backend API
# Mặc định: http://localhost:3001/api/v1
# 
# VÍ DỤ:
#   - Development (local): http://localhost:3001/api/v1
#   - Development (remote): http://192.168.1.100:3001/api/v1
#   - Production: https://api.yourdomain.com/api/v1
#   - Production (cùng domain): https://yourdomain.com/api/v1
# 
# LƯU Ý:
#   - Phải có prefix /api/v1 ở cuối
#   - Nếu dùng HTTPS, đảm bảo backend có SSL certificate
#   - Nếu backend và frontend cùng domain, có thể dùng relative path: /api/v1
#   - Kiểm tra CORS nếu backend và frontend khác domain
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Có sử dụng API thật hay không
# Giá trị: true | false
# Mặc định: false (development dùng mock data)
# 
# LƯU Ý:
#   - true: Kết nối với backend API thật
#   - false: Sử dụng mock data từ localStorage (chỉ development)
#   - Production: Tự động bắt buộc dùng API thật (bỏ qua giá trị này)
#   - Nếu VITE_API_BASE_URL có giá trị, sẽ tự động dùng API thật
VITE_USE_API=false

# ============================================
# CẤU HÌNH MÔI TRƯỜNG (TỰ ĐỘNG)
# ============================================
# Các biến sau được Vite tự động quản lý, không cần cấu hình:
# 
# - MODE: development | production
# - PROD: true | false (production mode)
# - DEV: true | false (development mode)
# - BASE_URL: Base public path
# 
# ============================================
# GHI CHÚ QUAN TRỌNG
# ============================================
# 
# 1. DEVELOPMENT MODE:
#    - Mặc định sử dụng mock data (localStorage)
#    - Có thể bật VITE_USE_API=true để test với backend
#    - Hot reload tự động khi code thay đổi
#    - Có thể debug trong browser DevTools
#
# 2. PRODUCTION MODE:
#    - BẮT BUỘC sử dụng API thật (không dùng mock)
#    - Phải cấu hình VITE_API_BASE_URL đúng
#    - Build file tĩnh, không có hot reload
#    - Tối ưu hóa code và assets
#
# 3. CORS VÀ PROXY:
#    - Development: Vite tự động proxy /api đến backend (xem vite.config.ts)
#    - Production: Cần cấu hình Nginx reverse proxy hoặc CORS trên backend
#    - Nếu gặp lỗi CORS, kiểm tra cấu hình backend CORS
#
# 4. TROUBLESHOOTING:
#    - Kiểm tra console browser để xem API URL đang dùng
#    - Kiểm tra Network tab để xem request có gửi đúng không
#    - Kiểm tra backend có chạy và accessible không
#    - Xem log backend để debug lỗi API
#
# 5. HỖ TRỢ:
#    - Xem tài liệu: README.md
#    - Xem hướng dẫn triển khai: 部署指南.md
#    - GitHub Issues: https://github.com/mrtinhnguyen/CRM/issues
#
# ============================================

